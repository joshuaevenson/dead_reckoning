{
  "name": "dead_reckoning",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-scenario-catalog.mjs && tsc -p tsconfig.json && vite build",
    "dev": "npm run build && node scripts/dev.mjs",
    "dev:backend": "node scripts/dev-server.mjs",
    "test": "npm run build && node --test test/*.test.js"
  },
  "dependencies": {
    "@primeuix/themes": "^2.0.3",
    "@primevue/themes": "^4.5.4",
    "@vitejs/plugin-vue": "^6.0.7",
    "primeicons": "^7.0.0",
    "primevue": "^4.5.5",
    "vite": "^8.0.16",
    "vue": "^3.5.38"
  }
}
